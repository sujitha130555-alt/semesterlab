name: Folder Specific Workflow

on:
push:
paths:
- 'src/**'

jobs:
build:
runs-on: ubuntu-latest

```
steps:
  - name: Checkout code
    uses: actions/checkout@v3

  - name: Run build
    run: echo "Changes detected in src folder. Running workflow..."
``` no hutton show fo ceck the freen sucess what i shoul doo how to show output i already commit add push 
